{
  "name": "eol-host-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo \"Add frontend tests\" && exit 0"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
